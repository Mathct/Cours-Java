## Semaine 3 – Persistance avec Spring Data JPA

Objectif global : brancher une vraie base SQL, mapper les entités avec JPA/Hibernate, et remplacer le stockage en mémoire par un `JpaRepository`.

## Plan de la semaine

- Jour 1 : SQL/JPA foundations + config BDD
- Jour 2 : Entité `Task` et mapping JPA
- Jour 3 : `JpaRepository` et CRUD
- Jour 4 : Relations JPA (`User` ↔ `Task`)
- Jour 5 : Requêtes dérivées + pagination/sort
- Jour 6 : Migrations (Flyway) + profils env
- Jour 7 : Mini-projet persisté complet

